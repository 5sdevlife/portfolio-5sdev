import { ReactElement } from "react";

const KeyArrowFunction = (): ReactElement<HTMLSpanElement> => {
  return <span className="arrow-function-key">{"=>"}</span>;
};

export default KeyArrowFunction;